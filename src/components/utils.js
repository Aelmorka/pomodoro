export const initialWorkTimer = {
    minutes: 25,
    seconds: 0,
    isWorkMode: true,
    isActive: false
}
export const initialBreakTimer = {
    minutes: 5,
    seconds: 0,
    isWorkMode: false,
    isActive: false
}